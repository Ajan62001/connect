"""Story mode — grounded narrative synthesis over an existing fact-set.

A story is a dossier(kind='story'): a synthesis-only pipeline (no research
loop) that gathers a CLOSED fact-set for a chosen subject (a story thread, an
investigation, a workspace, or a topic), then writes a cited narrative with the
arc *what happened -> what it was -> why -> how it affects*. Every citation
resolves to the closed evidence menu and is verified in code — the same
grounding discipline as investigation synthesis.
"""
